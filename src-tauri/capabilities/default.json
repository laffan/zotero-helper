{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main window and PDF capture windows",
  "windows": ["main", "capture-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "dialog:default",
    "opener:default",
    "capture-view:default"
  ]
}
